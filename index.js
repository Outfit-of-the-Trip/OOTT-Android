/**
 * @format
 */

import {AppRegistry} from 'react-native';
import RecoilApp from './src/RecoilApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => RecoilApp);
